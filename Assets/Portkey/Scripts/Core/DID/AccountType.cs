namespace Portkey.Core
{
    /// <summary>
    /// For the type of account when logging in or signing up.
    /// </summary>
    public enum AccountType
    {
        Email,
        Phone,
        Google,
        Apple
    }
}